import {
  TriggerNode,
  ConditionNode,
  ActionNode,
  TransformNode,
} from "./NodeTypes";

export const nodeTypes = {
  trigger: TriggerNode,
  condition: ConditionNode,
  action: ActionNode,
  transform: TransformNode,
};
